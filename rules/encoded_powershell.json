{
    "name": "encoded powershell execution",
    "rules": [
        {
            "processes": ["powershell.exe", "pwsh.exe"],
            "indicators": ["-encodedcommand", "-enco", "-enc", "-ec"],
            "mitre": "T1059.001",
            "severity": "high"
        }
    ]
}